{
  "name": "agentskillsforall",
  "private": true,
  "scripts": {
    "build": "cd website && npm install && npm run build",
    "dev": "cd website && npm run dev"
  },
  "dependencies": {
    "next": "^16.2.3"
  }
}
